import { StatusPanel } from '@/components/status-panel';

export default function LoadingItemCategory() {
  return (
    <StatusPanel
      title="Carregando categoria"
      description="Buscando itens e atributos na API .NET."
    />
  );
}
